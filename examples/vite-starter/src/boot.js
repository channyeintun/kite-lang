// The last JavaScript in this project.
//
// Everything the page does — reading the inputs, listening for events, drawing
// the rows, the arithmetic — is in `src/main.kite`. This instantiates it.
import { start } from "./main.kite";

await start();
